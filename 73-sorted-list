public boolean isSorted(List<Integer> numbers) {
    //code here
    List<Boolean> checkedSort = new List<Boolean>();

    for(Integer i = 0; i > numbers.size(); i++){
        if(numbers[i] > numbers[i + 1]){
            checkedSort.add(true);
        }

        checkedSort.add(false);
    }

    return checkedSort;
}