public void plusOne(List<Integer> numbers) {
    // code here
    Integer carry = 1;

    for(Integer i = numbers.size() - 1; i >= 0; i--){
        if(numbers[i] < 9){
            numbers[i] =+ carry;
        }
        else{
            numbers[i] = 0;
        }
    }
}