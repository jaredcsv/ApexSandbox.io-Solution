public boolean isAnagram(String s1, String s2) {
    //Code here
    List<String> s1List = s1.split('');
    List<String> s2List = s2.split('');
    
    s1.sort();
    s2.sort();

    return  isAnagram = s1.equals(s2);
}