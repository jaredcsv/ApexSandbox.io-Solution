public Integer maxConsecutiveOnes(Integer[] numbers) {
    //code here
    Integer onecounter = 0;
    Integer zerocounter = 0;
    Integer maxcount = 0;
    List<Integer> onerows = new List<Integer>();

    for(Integer i = 0; i < numbers.size(); i++){
        if(numbers[i] == 1){
            onecounter++;
        }
        else if(i == 0){
            zerocounter++;
            onerows.add(counter);
        }
    }
    
    onerows.sort();
    return onerows[onerows.size() - 1];
}