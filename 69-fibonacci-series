public List<Integer> fibonacciSeries(Integer n) {
    //code here
    List<Integer> secuenceNums = new List<Integer>();
    Integer prev, curr, past;

    if(n <= 2) secuenceNums.add(1);

    return secuenceNums;

    prev = 0;
    curr = 1;

    for (Integer i = 3; i <= n; i++) {
        Integer next = prev + curr;
        sequenceNums.add(next);
        prev = curr;
        curr = next;
    }
}