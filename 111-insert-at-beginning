public void insertAtStart(String cityName, List<String> cities) {
    //code here  
    if(cityName != null){
        for(Integer i = cities.size() - 1; i >= 0; i--){
            cities.set(i + 1, cities[i]); // Desplaza cada elemento hacia la derecha
        }
        cities.add(cityName); // Inserta cityName al principio de la lista
    }
}