public void insertAtStart(String cityName, List<String> cities) {
    //code here
    for(Integer i = 0; i < cities.size(); i++){
        cities[i] += cities[i++];
        cities[0] = (cityName); 
    }

    if(cities == cities){
    }
}