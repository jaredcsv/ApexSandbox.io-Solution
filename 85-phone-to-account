public Map<String, Account> phoneToAccount(List<Account> accounts) {
    //code here
    Map<String, Account> accWithPhone = new Map<String, Account>();

    for(Account acc : accounts){
        if(acc.phone != null){
            accWithPhone.put(acc.Phone, acc.Name);
        }
    }

    return accWithPhone;
}