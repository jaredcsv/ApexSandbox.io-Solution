public Set<Id> accountIds(List<Opportunity> opps) {
    //code here
    Set<Id> extractedIds = new Set<Id>();

    for(Integer i = opps.size() - 1; i > 0; i--){
        if(opps[i].Id == opps[i - 1].Id){
            extractedIds.add(opps[i].Id);
        }
    }

    return extractedIds;
}