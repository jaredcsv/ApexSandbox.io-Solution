public Decimal average(List<Decimal> numbers){
    // your code here
    List<Decimal> roundedNums = List<Decimal>();

    for(Integer i = 0; i < numbers.size(); i++){
    }
    
    return average(roundedNums);
}